<h2>{{ animal.name }}</h2>
<img src="{{asset('upload/' ~ animal.img)}}" alt="illustration" />
<span>{{ animal.typeId.Name }}</span>
<span>{{ animal.genreId.Name }}</span>
<a href="{{ path('app_reviews', { id: animal.id }) }}">Ajouter un avis</a>
{% include "animals/delete.html.twig" %}
<a href="{{ path('edit_animal', {id: animal.id}) }}">Modifier l'article</a>
{# <p>{{ animal.reviewsId.review }}</p> #}
